<%= image_tag 'Slyde_logo.jpg', :width => "300px"%>
<br>
<br>
<h1>Editing deck</h1>

<%= render 'form' %>

<h2>Attach Deck with Paperclip</h2>
<% form_for :deck, @deck, :url => decks_path, :html => { :multipart => true } do |form| %>
  
  <%= form.file_field :doc %>
  <%= submit_tag value="Submit" %>
<% end %>


<%= link_to 'Show', @deck %> |
<%= link_to 'Back', decks_path %>
